# Render.com blueprint — API + PostgreSQL
# Dashboard → New → Blueprint → connect afair86/cadence

services:
  - type: web
    name: cadence-afair86-api
    runtime: node
    plan: free
    buildCommand: >-
      corepack enable &&
      pnpm install &&
      pnpm --filter @cadence/shared build &&
      pnpm --filter @cadence/api build &&
      pnpm --filter @cadence/api exec prisma generate &&
      pnpm --filter @cadence/api exec prisma db push &&
      pnpm --filter @cadence/api db:seed
    startCommand: pnpm --filter @cadence/api start
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: cadence-afair86-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_VERSION
        value: "22"
      - key: PUBLIC_API_URL
        value: https://cadence-afair86-api.onrender.com

databases:
  - name: cadence-afair86-db
    plan: free
